{
  "$defs": {
    "AOCS": {
      "description": "Model for the Attitude and Orbit Control System (AOCS).",
      "properties": {
        "three_axis_stabilized": {
          "title": "Three Axis Stabilized",
          "type": "boolean"
        }
      },
      "required": [
        "three_axis_stabilized"
      ],
      "title": "AOCS",
      "type": "object"
    },
    "Communication": {
      "properties": {
        "GroundStation": {
          "$ref": "#/$defs/GroundStation"
        },
        "CommunicationSubsystem": {
          "$ref": "#/$defs/CommunicationSubsystem"
        }
      },
      "required": [
        "GroundStation",
        "CommunicationSubsystem"
      ],
      "title": "Communication",
      "type": "object"
    },
    "CommunicationSubsystem": {
      "description": "Model for the communication subsystem.",
      "properties": {
        "frequency": {
          "title": "Frequency",
          "type": "number"
        }
      },
      "required": [
        "frequency"
      ],
      "title": "CommunicationSubsystem",
      "type": "object"
    },
    "GroundStation": {
      "description": "Model for the ground station.",
      "properties": {
        "location": {
          "title": "Location",
          "type": "string"
        },
        "antenna_size": {
          "title": "Antenna Size",
          "type": "number"
        }
      },
      "required": [
        "location",
        "antenna_size"
      ],
      "title": "GroundStation",
      "type": "object"
    }
  },
  "properties": {
    "AOCS": {
      "$ref": "#/$defs/AOCS"
    },
    "Communication": {
      "$ref": "#/$defs/Communication"
    }
  },
  "required": [
    "AOCS",
    "Communication"
  ],
  "title": "Satellite",
  "type": "object"
}
